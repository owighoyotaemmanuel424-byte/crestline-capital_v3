# Set by `npx convex dev` or your deployment pipeline
NEXT_PUBLIC_CONVEX_URL=

# Production authentication / provider secrets belong in your hosting provider,
# never in source control. Add your selected identity, email, KYC, fraud and
# payment-provider variables here as the integrations are implemented.
